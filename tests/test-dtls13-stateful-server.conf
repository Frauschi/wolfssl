# server: stateful DTLS 1.3 (keeps state from the first ClientHello, 1-RTT, no
# stateless HRR cookie)
-u
-v 4
--dtls13-stateful-server

# client: DTLS 1.3 with a classical group, so it sends a usable key share in the
# first ClientHello and no HelloRetryRequest is needed
-u
-v 4
